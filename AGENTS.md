# YakuLingo で作業するときの決まり

**「次にやること」を書いた文書は無い。** かつて `_docs/ロードマップ.md` があったが、
記述が実装から離れ、2026-08-11 に削除した。検証した時点で §4「これからやること」5件のうち
4件が既に直っており、残る1件も大半が決着済みだった。**古い計画書は、無いほうがましである。**

次にやることは、そのつど**動くアプリと実装から**決める。`_docs/` にある他の文書は
観察と経過の記録であって、要件でも計画でもない。

守るべき制約は次のとおり（出典はコードと `_docs/引き継ぎ_実機検証_2026-08-05.md` §5-3）。

- **外部送信前のマスクを外さない。** 対象は**数値のみ**。固有名詞はマスクしない
  （2026-08-14 に利用者判断。制約文のほうが古かった。`[[P` 形式は実装されたことが無く、
  `src/Server.ps1` の開示文も固有名詞を送ると明示している。
  出典 `_docs/決定_実装優先と固有名詞マスク_2026-08-14.md`）
- **アプリは共有フォルダへ書き込まない。** 書き込み先はすべてローカル（`src/Corpus.ps1:9`）
- **コーパスは足しであって前提ではない。** 引けなければ従来どおり訳す
- **Copilot が rate limit を返したら、そのまま再依頼しない。** ~~上限超過での再依頼はしない~~
  （2026-08-16 に利用者判断で書き直した。**呼び出し回数の設計上の上限は無い。**
  元の文は、以前に他アプリと同時へ大量投入して出たエラーを指していた。
  実装にも上限は無く、`src/CopilotBudget.ps1` は `Add-YakuCopilotCall` で数え、
  閾値を越えたら記録へ `note=near-usage-limit` を書くだけである。止める関数は無い。
  `Test-YakuCopilotLimitError:137` が応答の文面から `rate limit|too many requests|quota` を
  拾うのが唯一の検出で、これは**事後の扱い**である。
  なお設定の `copilotPromptCharLimit` は1回のプロンプトの文字数であって、回数とは無関係。
  **「上限があるから」を理由に呼び出し回数を惜しむ設計はしない。**
  ~~この理由付けは `src/CatProject.ps1:3585-3588` の註に残っているので、そちらも直すこと~~
  **2026-08-17 に直した。** `Get-YakuCatTranslationMemoryPretranslatePlan` の註である
  （行は動くので関数名で探すこと）。事前翻訳が効く理由は上限ではなく、
  **同じ原文へ同じ訳を返すこと**（何ページ訳しても去年と同じ注記に同じ英文が入る。
  Copilot は呼ぶたびに言い回しが揺れる）と、**往復が1回減って速いこと**の2つ）
- **数値が抜けた訳は警告ではなく欠陥。** 意図した省略かを利用者に判断させない
- **用語集の完全一致（cell-exact）は外さない。** 用語の一貫性ではなくレイアウトの保証
- **書き戻しDRAFTを止める理由は3つ。** ~~全行確認要件は緩めない~~ ~~止めるのは2つだけ~~
  （2026-08-12 に全行確認要件を外した。市販ツールを調べたところ memoQ・Phrase・Trados の
  どれも「書き出す」と「完了にする」を分けており、全行確認を書き出しの条件にして
  いたのは当アプリだけだった。2026-08-14 に「2つだけ」を実装に合わせて訂正。
  実装は最初から3つを積んでいた。出典 `_docs/決定_実装優先と固有名詞マスク_2026-08-14.md`）
  - `segment-untranslated` — 訳文が空の行がある → 出さない
  - `segment-qc-failed` — `Invoke-YakuCatSegmentValidation` の error が1件以上 → 出さない。
    **数値だけではない。** `currency-mismatch` `structure-integrity` `structure-validation-error`
    `terminology-conflict` `terminology-forbidden` `terminology-missing` も error である
  - `segment-qc-not-current` — 確定済みだが用語スナップショットが変わり、点検が古い → 出さない
  - **未確認は止めない。** 代わりに何行未確認かを、押す前の画面とファイルの中の
    両方に必ず書く。点検は確定時にしか走らないので、未確認の行は出力時に写しへ
    走らせて判定する
  - 止まった理由の**表示**は種別ごとに分けた（2026-08-15 `dca2558`）。error コードは
    実測**18種**で、それぞれに文言がある。直すのは `Get-YakuCatOutputPreflight`。
    **`Get-YakuCatExportPreflight` という関数は実在しない**（この行が長らくその名前で
    宿題を書いていた。2026-08-15 に訂正）
  - ~~**宿題の残り半分。** 文言が案内する「点検の指摘」が、その場面の画面に無い~~
    **2026-08-17 に確認したところ、既に片づいていた。** 写しの結果を捨てずに
    持ち回る形が入っている。この項目のほうが古かった。

        サーバ  Get-YakuCatOutputEligibility → QcRows（写しの点検結果）
                Get-YakuCatProjectView       → 行ごとの qc_preview
        画面    qcMessages() が qc_findings と qc_preview を併せる
                → counts.qc → 絞り込みボタンの表示
        試験    qc_preview を固定しているのは5本（実機Chromium を含む）

**制約文と実装が食い違ったら、実装を採る。** 制約文のほうを直す（2026-08-14 の利用者判断）。
これは制約を緩めてよいという意味ではない。実装を制約文へ合わせて変える改修は、この決まりを
根拠にしては行わない。

## 悪い結果が出たら、まず自分の道具を疑う

これは一般論ではなく、2026-08-07 の1日で7回同じ失敗をした結果の決まりである。
毎回「相手（Copilot / Edge / 利用者の環境）が悪い」と説明し、毎回それが誤りだった。

**説明を立てる前に、相手が実際に何を返したかを読む。** 順序は必ずこれ。

1. 生の出力を読む（ログ、応答本文、プロセス一覧、設定ファイル）
2. 自分の測定・照合・監視が正しいか確かめる
3. そのうえで仮説を立てる

1 と 2 を飛ばして 3 をやったものは、これまで全部外れている。

### 実際に外した例（すべて同じ日）

| 私の説明 | 実際 |
| --- | --- |
| Copilot の対応づけが4割誤り | 私の照合が単位換算（億円↔billion）を見ていなかった。実際は 15/15 正しい |
| Copilot 画面で何かを待っている | 何も待っていない。ログとプロセスを見れば分かった |
| 他のアプリが Copilot を掴んでいる | ポートもプロファイルも分かれていた |
| 用語集に不正な `oku` が混入 | `億円,oku` は正規のエントリ。単位規約そのもの |
| 外部から CDP を突いて凍結を調べる | CDP は1ページ1クライアント。原理的に不可能 |
| バッチがスタックした | 3時間前に完走していた。監視が CP932 のログを UTF-8 で探していた |
| 連続使用で Copilot が劣化する | Copilot は12秒で4,518文字を返していた。解析側の問題 |

### 具体的な確認先

- アプリのログ: `~/.yakulingo-ps/logs/yakulingo.log`
  失敗時は `Copilot wait result` / `salvaged` / `reason=` を見る。
  応答本文が要るときは設定 `diagnostics_level: full`（終わったら戻す）
- Copilot 用 Edge: ポートは設定 `edge_debug_port`（既定 9433。9222 ではない）
  プロファイルは `~/.yakulingo-ps/edge-profile`
- 他アプリとの競合を疑う前に `Get-CimInstance Win32_Process` で
  実際のコマンドラインとポートを見る

## 根拠を引くときは、引用元を開いて読む

2026-08-11 に、当時あった `_docs/ロードマップ.md` §8 を根拠に「一括確定は採らない」と決めた。
§8 は実在し、引いた行も実在した。**しかしその行は、私が述べたことを述べていなかった。**

- 引いた行は「数値が抜けた訳は警告ではなく欠陥」＝数値QCをブロッカーにする決定
- 私はそれを「確認の粒度」の決定として使った。無関係だった
- しかも「1文ずつ確認するのが値打ち」という前提の唯一の出典は、**同じ日に私自身が
  実機テスト記録へ書いた1行**だった。決定事項ではない

**自分が書いた文を、翌日「要件」として引用しない。** 引用するときは、引用元の節を
開いて、その行が実際に何を述べているかを読む。`_docs/` の記録は観察と経過であって、
決定は §5・§7・§8 にしかない。

判断を「値打ち」「思想」「前提」といった言葉で支えたくなったら、それは根拠が
無い合図である。そのときは実装を読む。今回は `Ctrl+Enter` が確定後に次行へ自動で
移ることに気づいた時点で、前提が最初から成立していないと分かった。

## 画面を変えたら、実機で開いて確かめる

「画面は開いて押す」という決まりを、2026-08-11 に自分で破った。
ブラウザでの実測（行の高さ、幅の配分）だけで PR を出し、実機で開いたら左ナビの
ラベルが3行に折り返していた。

- **実測しても、測る対象を絞りすぎると外す。** 変えた値だけでなく、**その値に
  依存しているものが壊れていないか**まで測る。幅を詰めたなら「ラベルはまだ1行に
  収まるか」を測る
- 寸法を決める前に、いちばん長いラベルの実測幅を出す。`点検で気になる点` は
  119px 必要で、詰めた幅では 65px しか与えられていなかった
- ~~実機の窓幅は約 1380px（`@media (max-width: 1400px)` に当たる）~~
  **これは誤りだった。2026-08-16 に利用者の機械で実測して訂正した。**

      screen 1920x1200 / 最大化した窓 inner 1912x987 / devicePixelRatio 1 / zoom 1
      matchMedia('(max-width: 1400px)').matches = false

  **利用者の画面では、狭い版のレイアウトは一度も当たらない。**
  1380 という数字は測定ではなく、`@media (max-width: 1400px)` を踏ませたくて
  選んだ値だった。それが AGENTS.md・`tools/Measure-YakuUiFloor.ps1`・
  `tools/cat-screen/cat-screen-gate.js`・`tools/Smoke-Test.ps1` へ
  「実機」として転記され、繰り返しによって事実になっていた
  （出どころを問われて初めて追った。**引用元を開いて読む**の再発である）。

  実際、1380 で測ると上部の操作ブロックが折り返して 184px → 408px になり、
  「格子より上が画面の 64%」という、利用者が決して見ない数字が出る。
  実機の 1912px では 36% である。**2倍近く外していた。**

  - **画面の寸法を語るときは、利用者の機械で測った値を使う。**
    狭い窓の版を守る試験は残してよいが、それを「実機」と呼ばない
  - `Measure-YakuUiFloor.ps1` と `cat-screen-gate.js` は 1380x900 固定である。
    実機幅での測定を足すこと（狭い版の代わりにするのではなく、両方）

## CSS の詳細度も「自分の道具」である

2026-08-11 に、無効ボタンの色を直すため `styles.css` の塗りの基本ルールへ
`:not([disabled])` を1つ足した。詳細度が (0,3,1) から **(0,4,1)** に上がり、
CAT 側の部品の上書き（いずれも (0,3,1)）を**全部踏み潰した**。左ナビも点検タブも
一覧の行も accent で塗られ、件数のコントラストは **1.05:1** まで落ちた。

- `:not()` は連ねると1つにつき class 1個ぶん積む。**リスト形なら引数の最大値1つぶん**。
  `button:not(.a, .b, .c, [disabled])` は (0,1,1)
- 状態（`:hover` `[disabled]`）を足すたびに詳細度を継ぎ足す作りにしない。
  基本ルールを低いままにして、部品側に勝たせる
- 見た目がおかしいときは、**勝っているセレクタを数える**。数えれば一発で分かる

## 検証の道具そのものを疑う（実際に踏んだもの）

- **試験を1プロセスで回さない。** `foreach` の中で `& $_.FullName` すると
  `$LASTEXITCODE` と状態が引き継がれ、緑の試験が赤に見える。実際 33本中3本を
  誤って赤と報告した。`powershell.exe -File` で1本ずつ別プロセスにすると全部緑だった
- **bash の heredoc は `
` を実改行に変える。** `window.confirm('...

...')` の
  ような JS 文字列を python heredoc 経由で書くと構文エラーになる。この罠は2回踏んだ。
  **JS の文字列リテラルを含む変更は Edit ツールで書く**
- 数値を語るときは、どの窓幅・どの文字サイズで測ったかを併記する
- **`Start-Process -PassThru` は `-Wait` を付けないと `.ExitCode` が `$null` になる。**
  プロセスハンドルが保持されないため。`$null = $p.Handle` を Start-Process の直後に
  置くと読める（実測: 触らない側 `null` / 触った側 `7`）。`-Wait` で待つ側だけ
  取れてしまうので、掃引の中で1本だけ `-Wait`、残りを `WaitForExit(ms)` にすると、
  その1本だけ正しく他が全部空になる。2026-08-15 にこれで **緑の回帰54本を
  「54本赤」と報告しかけた**。空を「0以外」として赤へ畳んでいたためである
- **「測れなかった」を「赤」に畳まない。** 緑・赤・未測定は3つの状態として数える。
  畳むと、道具の故障が対象の欠陥に化ける。`Measure-YakuUiFloor.ps1` と
  `Test-YakuV9176CatScreenWiring.ps1` が終了コードを 0 / 1 / **3（未測定）** に
  分けているのはこのため
- **PowerShell の関数の中で、書式化した文字列を裸で置くと戻り値に混ざる。**
  `"{0} exit={1}" -f ...` と `return $p.ExitCode` を並べると、呼び出し側は
  `@('... exit=0', 0)` を受け取る。比較が黙って壊れ、表示行も出なくなる。
  表示は `Write-Host` で出し、戻り値は `[int]` へ明示的に落とす
- **集計の regex は、リダイレクト先の生バイトを見てから書く。**
  `RedirectStandardOutput` の出力は改行が CR 単独になることがあり、`Get-Content` が
  複数行を1行に畳む。`^\s*ok\s` のような行頭固定の数え方は、そこで大きく外す
  （2026-08-15 に実測 183件を 55件と数えた）。**終了コードで判定し、
  件数は補助情報として扱う**

## 数字が悪いときに疑う順序

1. 照合・集計の実装（単位換算、桁区切り、全角、日付の誤結合）
2. 試験データの作り方（正解が自分の出力由来になっていないか）
3. そのあとで対象の性能

## 監視・待機

- ログを機械で待つときは、**自分で書いた UTF-8 ファイル**に ASCII の目印
  （`DONE` / `NG`）を置いて、それを探す。目印が ASCII なら読み方を間違えても当たる。
- **`RedirectStandardOutput` の符号化は決め打ちできない。開く前に生バイトを見る。**
  2026-08-15 に12本を実測したところ **11本が CP932**（先頭の非 ASCII が `90 94 82 A6`
  ＝「数え」）で、UTF-8 として復号できなかった。残る1本は非 ASCII が0個の ASCII のみ。
  一方、同じ日に別の使い捨てスクリプトから起動したぶんは `E6 95 B0` で UTF-8 だった。
  **起動元の状態で変わる。** 目印を ASCII にしておけばどちらでも当たる。
- **符号化を判定するときは、文字数を数えない。生バイトを見るか、厳密 UTF-8 として
  復号できるかで決める。** 「復号して日本語が多く読めたほう」で判定すると、
  UTF-8 を CP932 で読んだ化けが CJK 範囲に落ちるぶん、**誤った側が勝つ**。
  2026-08-15 にこの判定法で「CP932 だ」と確信し、生バイトを見て逆と分かり、
  さらに12本で測り直して**元の CP932 が正しかった**と分かった。
  1本で決めない。判定法そのものを先に疑う。
- 待つ前に「この条件は本当に発火するか」を一度確かめる。

## PowerShell 5.1 の落とし穴（このリポジトリで実際に踏んだもの）

- `[int](3/2)` は 2。切り捨ては `[Math]::Floor`
- `@($a - $b, $a + $b)` はカンマが先に結合する。括弧を明示する
- `@($null)` は要素1の配列
- `@(Get-Content x | ConvertFrom-Json)` は配列全体が要素1になる。
  一度変数へ受けてから `@($var)`
- **`@($list)` は `List[object]` に限って落ちる。** `ArgumentException:
  Argument types do not match`。`List[string]` も `List[int]` も `List[psobject]` も
  `ArrayList` も落ちない。`foreach` も `[object[]]` も `.ToArray()` も落ちない。
  `@()` だけが落ちる（実測 5.1.26100.9168）。**`.ToArray()` で書く。**
  2026-08-13 に `src/SheetLayout.ps1` がこれで1枚も返しておらず、
  しかも `catch { return @() }` の内側だったので無音だった
- 関数が `return ,$array` で返したものを `@()` で包むと入れ子になる
- **配列パラメータに `[Parameter(Mandatory=$true)]` を付けると、5.1 は
  「空か」を判定するために全要素をたどる。** 5MB の `byte[]` で1回 **185ms**
  （実測 5.1.26100.9168）。中身の SHA-256 そのものは 1.3ms なので、
  費用の99%が空判定だった。`[AllowEmptyCollection()]` 単独や `[object]` は無害。
  2026-08-14 に TM の追記が 11ms のはずが 557ms になっていた原因がこれ。
  **遅いときは算法を疑う前に、パラメータ属性を見る**
- `-not (X) -match Y` は `(-not X) -match Y`。括弧を明示する
- `[IO.File]` は `Set-Location` を見ない。絶対パスを渡す

## 符号化

- `.ps1` は UTF-8 BOM 付き。BOM 無しで書くと日本語が化けて構文エラーになる
- **これは使い捨ての確認用スクリプトにも効く。** 一時ディレクトリへ新規に書いた
  `.ps1` は BOM が付かないことがあり、日本語のリテラルがそこで壊れる。
  2026-08-15 に、壊れた原文で関数を呼び、返ってきた値を根拠に結論を出しかけた。
  **確認用スクリプトは ASCII だけで書き、日本語が要るなら符号位置から組み立てる**
  （`[string]::Concat([char]0x578B, ...)`）。組み立てた文字列は、使う前に
  長さか符号位置を印字して、意図どおりかを確かめる
- 改行は `tools/eol-baseline.txt` の通り（`www/assets/app.js` は LF、多くは CRLF）
- 変更後は `tools/Check-Encoding.ps1` を通す

## 作業の締め

- **全回帰を回す前に、差分とレビュー指摘から影響範囲を決める。** 実装役とレビュー役を
  分けた作業では、まずレビュー役の判断を取り、ローカルでは変更箇所の重点試験を通す。
  `tools/Test-YakuV91*.ps1` 全件は、横断的な変更で必要なとき、または CI で通す。
  レビュー後の局所修正ごとに全件を回し直さない
- コミット前に `tools/Check-Encoding.ps1` と、影響範囲に対応する重点試験を通す
- `Test-YakuPackage.ps1` は `-PackagePath` が要る手動ツール。回帰テストではない
