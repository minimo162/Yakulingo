# YakuLingo ロードマップ

最終更新 2026-08-08 ／ **作業を終えるとき §0 と §1 だけ書き換える**

この文書は、何日か空けて開いたときに仕事を再開するためのものである。
報告書ではない。網羅より「読んで動ける」を優先する。

---

## §0 次の一手

**§4-1 CAT の単位変換。** CAT だけ `Convert-YakuNumericUnits` を通っておらず、
`18万6千台` が `[[N2]]万[[N3]]千台` の形で送られる。1つの数が2つの記号に割れ、
モデルは組み立てられない。ファイル翻訳タブを廃止して全員を寄せた主経路である。

やり方は §4-1 に書いた。**まず実機で1件流して、送っているものを目で見ること。**

---

## §1 いまどこにいるか

| | |
| --- | --- |
| 作業ブランチ | `claude/initial-repository-content-ursfxd` |
| 未マージの PR | [#4](https://github.com/minimo162/Yakulingo/pull/4) |
| `main` | V91.60（数値マスキング）＋ V91.61 段階1（コーパスの配布）まで |
| 回帰 | 24本。`tools/Check-Encoding.ps1` 94ファイル |
| **実機で見ていない** | **今日入れたものは全部**。短くするボタン、過去の英訳の折りたたみ、CAT のマスク、出力の停止 |
| 起動と置き場所 | `_docs/引き継ぎ_実機検証_2026-08-05.md` §3 |

**画面は開いて押していない。** 今日、構文検査と回帰テストを通り抜けた不具合が
5件見つかっている（§6）。ここが最大の未確認事項である。

---

## §2 何を解こうとしているか

利用者が挙げた課題（2026-08-08）。これが基準であり、ここに紐づかない限り機能は足さない。

| | 課題 | いまの状態 |
| --- | --- | --- |
| 1 | **短い訳の品質が低い。**用語集の機械置換と Copilot の英訳で訳が揺れる | 訳を1本にし、短くするのを派生にした。揺れの後半には**まだ何も向いていない**（§5-2） |
| 2 | **CAT ツールではない。**さきほど訳した文と今の文が整合しているか分からない | 効いているのは候補ペインの「自分の訳」だけ。完全一致と包含しか当たらない |
| 3 | **過去の翻訳例が見れない** | 3,001組。訳文の下に日英で出す。**ただし配布すると0件**（§4-3） |
| 4 | **英文短信・英文有報向けの翻訳もしたい** | **紐づく項目が無い**（§5-3） |

---

## §3 決めた線引き

**簡易翻訳は読むだけ。書かない。** 迷ったら「溜まるかどうか」で決める。溜まるものは CAT。

| | 簡易翻訳 | CAT |
| --- | --- | --- |
| 訳す | ○（1本） | ○ |
| 短くする | ○（押したときだけ） | 資料ごとに決める |
| 推敲の往復 | ○ | ○ |
| 公表訳を読む | ○ | ○ |
| 翻訳メモリ（書く） | × | ○ |
| 機械的な検査 | × | ○ |
| 確定・保存・再開 | × | ○ |

---

## §4 これからやること

### §4-1 CAT の単位変換 ← 次はこれ

| | |
| --- | --- |
| 何が問題か | CAT だけ単位変換を通らない。`18万6千台` → `[[N2]]万[[N3]]千台`。1つの数が2つの記号に割れる |
| なぜ起きるか | `Convert-YakuNumericUnits` は `Invoke-YakuFileTranslation`（`FileTranslation.ps1:1637`）の中にあり、CAT が呼ぶ `Invoke-YakuFileTranslationItems`（同 1288）には無い |
| 同じ抜け方 | 数値マスク、固有名詞マスクと**まったく同じ**。中継関数を直接呼ぶ経路が漏れる |
| 先にやること | 実機で1件流し、`diagnostics_level: full` でプロンプト本文を読む |
| やり方 | `Protect-YakuCatItems`（`CatProject.ps1`）の中、固有名詞マスクの前に置く |
| 注意 | 回帰の CASE 3b「日本語の桁が残らない」は text 経路しか見ていない。CAT でも見るようにする |

### §4-2 書き戻しが原文を見ていない ＋ 再開すると Blocks が無い

**2件を1つの作業として直す。** 同じ根（セル番地を信用している）から出ている。

| | |
| --- | --- |
| 何が問題か | 取り込み後に行を足すと、訳文がずれたセルへ**黙って**入る。再開すると元の塊が無く出力できない |
| いまの状態 | 出力は止めてある（止血）。**直ってはいない** |
| 使える部品 | `CellAlign.ps1` が同じ結論に到達済み（錨＋最長増加部分列）。番地は使えないと冒頭に書いてある |
| 一括経路は安い | `FileProcessors.ps1` の 2047・2083 で現在値を既に読んでいる。同じループで原文と突き合わせられる |
| これが済むまで | **ECM の実機検証をやってはいけない**（§4-6）。1回きりの機会を、既知の不具合の再確認に使うことになる |

### §4-3 対訳が配布されない

| | |
| --- | --- |
| 何が問題か | 配布物に `pairs.jsonl` が入らない。しかも読み手が配布先を見ていない |
| 確かめたこと | 取り込み側で3件、配布物で0件。読み手3か所とも `Get-YakuCorpusBuildDir`（取り込んだ端末のローカル作業場所） |
| 影響 | 課題3のために作った3つ（訳文下の公表訳・候補ペイン・対訳検索）が、**アライメントを走らせた端末でしか動かない** |
| やること | `New-YakuCorpusPublishFolder` が pairs を配る。読み手を `Get-YakuCorpusSearchDir` 側へ寄せる |

### §4-4 推敲の往復（作業は入力欄を1つ置くだけ）

| | |
| --- | --- |
| 何が問題か | Copilot の訳は一発では不十分。原文と並べて具体的に指示して直す作業が実際に行われている |
| いまの状態 | **バックエンドは全部ある。**`/api/revise-text`、`text_revise.txt`、`Invoke-YakuTextRevision`、回帰テスト、実機検証済み |
| 欠けているもの | `data-yaku-revise` を出す HTML。`app.js:1372` が探しているが、どこも出力していない。**到達不能** |
| 置き場所 | CAT 側。簡易翻訳には意図して置かない（`Html.ps1` の `$reviseHtml = ''`） |
| 決めごと | 自由入力のみ。押しボタンは置かない（業界に前例が無く、根拠を集める手段もローカルアプリには無い） |

### §4-5 Copilot の使いすぎで作業が消える

| | |
| --- | --- |
| 何が問題か | CAT は途中結果を保存せず、キャッシュも読み書きしない。20バッチ中12で制限に当たると1〜11の訳が消え、消費した回数だけ残る |
| なぜ重いか | 再実行はキャッシュに当たらないので、同じ回数をもう一度使う。**残り回数が少ないときに消費が増える** |
| いまの状態 | 回数は数えている（`CopilotBudget.ps1`）が、出口は**ログだけ**。利用者には見えない |
| やること | ①押す前に「この操作で約N回使います／直近3時間でN回」を出す（材料は揃っている）②バッチごとに保存 ③CAT にキャッシュを繋ぐ（読み書き両方あり、呼ばれていないだけ） |

### §4-6 実務（外部の都合があるもの）

- **アライメントの完遂**（残り5冊：通期短信1・統合報告書2・有報2）
  段分けは `7adcea4` で入ったばかり。**1冊流して拾った実物を数件見てから**残り4冊へ
- **ECM の実機検証**（会社・連休明け）。**§4-2 が済んでから。** 書き戻し、図形・テキストボックス、行・列の増減への耐性

---

## §5 意図的にやらないこと

### §5-1 用語集をプロンプトから外すか

**もう決着している。** GLOSSARY 節は 2026-08-06 に廃止済み（`PromptBuilder.ps1:529`、
`prompt_glossary.csv` も同 985）。雛形に差し込み口は1つも無い。

一度これを「これからやること」に書いてしまったので、記録として残す。
根拠にしていた `実機検証結果_2026-08-05.md` §3-2 は、その節があった版の測定である。

### §5-2 用語集の適用漏れを検出する

判断が入る（言い換えを誤検出する）ので保留。

**ただしこれを保留にすると、課題1の後半（機械置換と Copilot 訳の揺れ）に
向くものが1つも無くなる。** 保留のままでよいかは、いつか決め直す必要がある。

### §5-3 課題4（英文短信・英文有報）を「翻訳」として解くこと

いまのアプリでは成立しない。足りないのは訳の質ではない。

| | |
| --- | --- |
| 入口 | 扱えるのはテキスト貼り付けと `.xlsx/.xlsm/.csv` だけ。短信・有報は Word・HTML・PDF・XBRL |
| 出口 | Excel か画面のコピー。短信の様式へ戻す道が無い |
| 文体 | 公表体の軸は 2026-08-08 に畳んだ。CAT は逆に常に社内の電文体 |
| 規模 | 有報1冊は数万字。バッチ3,000字なら数十〜百数十回。Copilot は120回前後で止まる |

**材料の側はむしろ揃っている。** 3,001組のうち 2,582組（86%）が決算短信、
268組が有報・有価証券報告書である。「過去の翻訳例」は課題3として作ったが、
中身の大半は課題4の材料になる。

### §5-4 付き合うしかない制約

- **Copilot の利用制限** — 121回ほどで応答しなくなり、時間を置くまで戻らない。
  Edge を作り直しても回復しない。**ただし「アプリ側の問題ではない」は
  「対処もアプリ側に無い」を意味しない**（§4-5）
- **Edge の凍結** — 18分のアイドル後にページ層だけが応答しなくなる。再現条件を掴めていない。
  外部から CDP を突く方法は使えない（1ページ1クライアント）。
  試していない手が1つある: 起動フラグにスリープタブを止めるものが入っていない

---

## §6 やって駄目だったこと（もう試さない）

- **外部から CDP を突いて凍結を調べる** — 1ページ1クライアントで原理的に不可能
- **転置索引** — 実測して捨てた（`V91.61_段階2_コーパス検索.md`）
- **字幕方式** — 無茶な翻訳になる
- **上限超過での再依頼** — 時間がかかり、無茶な翻訳になる（利用者の指示）
- **原文→BRIEF を並列で頼む** — 圧縮が日本語照合になり、用語集のカバー有無で
  圧縮率が 0.55 と 0.75 に割れた（`要件整理_汎用翻訳アプリとRAG翻訳.md` §5-A）
- **金額の billion 表記** — 桁の換算コードが無く、`122億円` が `¥122 billion` に
  なる（10倍）。換算を入れるまで選択肢から外してある

---

## §7 触ってはいけない場所

`_docs/引き継ぎ_実機検証_2026-08-05.md` §5-3 に一覧がある。特に:

- **外部送信前のマスクを外さない。** 数値・固有名詞
- **アプリは共有フォルダへ書き込まない**
- **コーパスは足しであって前提ではない。** 引けなければ従来どおり訳す
- 上限超過での再依頼はしない

なお §5-3 の「**設定項目を増やさない**（画面が汚れる）」は 2026-08-08 に更新された。
金額の書き方について「設定で切り替えて、簡単に目に入るところで」という指示が出ている。
古い記録のほうを読むと矛盾に見えるので、ここに書いておく。

---

## §8 決まったこと（もう議論しない）

| 決定 | 理由 | いつ |
| --- | --- | --- |
| **訳は1本。短くするのは押されたときだけの派生** | 原文→BRIEF を並べると圧縮が日本語照合になる。英文から縮めれば語彙依存が原理的に消える。加えて毎回2本作ると Copilot で使える文の数が半分になる | 2026-08-08 |
| **略語はモデルに選ばせず、一覧にあるものだけをアプリが当てる** | 採否は「綴ると入らない」かつ「一般的」の2基準。プロンプトの指示は保証にならない | 2026-08-08 |
| **簡易翻訳は読むだけ。書かない** | 溜まるものは CAT（§3） | 2026-08-08 |
| **固有名詞は送る前に置換して戻す** | 読みが自明でないものが多く、Copilot に任せると揺れる（毛籠→Kego など） | 2026-08-08 |
| **数値が抜けた訳は警告ではなく欠陥** | 社内資料でも致命的。意図した省略かを利用者に判断させない | 2026-08-08 |
| **ファイル翻訳タブを廃止し、全員を CAT へ** | 途中が見えないと崩壊に気づけず、あとの修正作業が増える | 2026-08-08 |
| **用語集の完全一致（cell-exact）は外さない** | 用語の一貫性ではなくレイアウトの保証だから（`決定_用語集は…`） | — |

---

## §9 作業の進め方

`CLAUDE.md` に決まりがある。**悪い結果が出たら、まず自分の道具を疑う。**

そのうえで、この文書に固有のもの:

- **画面は開いて押す。** 構文検査と回帰テストが通り抜けた不具合が、今日5件（§6の下）
- **指標を作ったら、その指標が拾った実物を数件見る**
- **件数を根拠にしない。** 用語はどちらかに統一されるので、出現回数は
  「その語が何回話題になったか」でしかない

### 検査を通り抜けた不具合の型（2026-08-08）

単体は全部動いていて、壊れているのは**繋ぎ目**だった。

| 何が壊れていたか | なぜ通り抜けたか |
| --- | --- |
| 突き合わせボタンが押した瞬間に落ちる（`yakuPostJson` 未定義） | 同期例外で catch に入らず、「数分かかります」が失敗を隠した |
| 候補ペインがマウスで押しても無反応 | focusin が click より先に走り、現在行の印が消えていた |
| 簡易翻訳から CAT へ渡すとき訳文が捨てられる | 単体はどちらも動く |
| 翻訳方向が黙って逆になる | ラジオは「自動」のままなのに、そこを見ていた |
| CAT のボタンが準備前に無反応 | 条件に合わないと何も言わずに戻っていた |
| **CAT が実数値と人名を素のまま送っていた** | マスクが**呼び出し元0件の関数**にあり、検査はそのファイルを grep していた |

最後の1件が最も重い。**検査が、閉じようとした穴と同じ形の穴を持っていた。**
いまは中継関数を「自分で伏せるもの」と「呼び出し側に任せるもの」に分けて見ている。

---

## §10 文書の地図

| 知りたいこと | 読むもの |
| --- | --- |
| 全体像と次の一手 | この文書 |
| 作業の決まり（符号化・回帰・失敗の型） | `CLAUDE.md` |
| 起動と置き場所、触ってはいけない決定 | `引き継ぎ_実機検証_2026-08-05.md` §3, §5-3 |
| 用語集とコーパスの効き方（実測） | `実機検証結果_2026-08-05.md` §3-1, §3-2 |
| 並列／直列と圧縮率の実測 | `要件整理_汎用翻訳アプリとRAG翻訳.md` §5-A |
| 用語集を何のために使うか | `決定_用語集は用語の一貫性ではなくレイアウトの保証.md` |
| アライメントの手法 | `日英アライメント手法_2026-08-06.md` |

**節を参照するときは必ず文書名を書く。** `§3-1` だけでは `実機検証結果` と
`要件整理` の両方にあり、中身が違う。間違えて開いても「それらしいもの」が
出てくるので、誤読に気づけない。
