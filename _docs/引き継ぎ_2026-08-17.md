# 引き継ぎ（2026-08-17）

この回でやったことと、次に読む順。**前回の引き継ぎは `_docs/引き継ぎ_2026-08-16.md`。**

コミットは7本。`git log --oneline b3257da..15aa681` で並ぶ。回帰は掃引71本すべて緑、
符号化157件通過（最終確認は `15aa681` 時点）。

---

## 0. まず読むもの

| 目的 | 文書 |
| --- | --- |
| **いま何をしようとしているか** | `_docs/決定_セルに収める_2026-08-17.md` ← これが本体 |
| 5経路の訳し比べ（散文） | `_docs/測定_通し比較_2026-08-17.md` |
| 表のラベルでの比較と、その物差しの限界 | `_docs/測定_圧縮された原文_2026-08-17.md` |
| Copilotへ実際に出ている量 | `_docs/測定_プロンプトの大きさ_2026-08-17.md` |
| 題材（訳文入り） | `_docs/比較題材_Smartcat通し比較_2026-08-17.json`（散文20文）<br>`_docs/比較題材_圧縮された原文_2026-08-17.json`（表のラベル20行） |

---

## 1. この回で直した欠陥（4件、すべて回帰つき）

| コミット | 欠陥 | 実測 |
| --- | --- | --- |
| `78021b9` | **背面タブでCopilotが止まる** | Edge が隠れたタブのタイマーを止め、`await sleep` を含むスクリプトだけが返らない。生存確認が `1+1` で、凍ったタブでも97msで通っていた |
| `c2abca7` | **英訳へ全角数字が出る** | 表7/20・散文6/20。他3ツールと公表訳は0/20 |
| `35414bc` | **PDF照合が答えを捨てる** | 掲載箇所すべてに同じ警告。`match_count` は記録されるのに使われない |
| `15aa681` | **略語の往復が閉じない** | 略語を登録した瞬間に候補生成が丸ごと落ちる。試験は素の `entry_id` で組むので緑のまま |

回帰: `Test-YakuV9183CopilotTabVisibility` / `Test-YakuV9184FullWidthRestore` /
`Test-YakuV9185WidthMeasurement` / 既存の `Test-YakuV9170PdfCompleteness` を更新。
**4件とも壊して赤・戻してハッシュ一致で緑**を確認済み。

## 2. 段階0で入れたもの（`be78946`）

幅の物差しが2か所で甘い側へ外れていた。

```
測る側  www/assets/cat.js  14.7px Calibri 固定
書く側  src/Settings.ps1   Arial（和→英）/ MS Pゴシック（英→和）
        Arial のほうが広い → 「収まる」と言い過ぎる
```

- 測定書体を出力書体にした（`__YAKU_OUTPUT_FONT__` / `_JP__` を Server から渡す）
- `SheetLayout.ps1` が `shrinkToFit` を読むようにした
- `width` 属性の無い `<col>` を `unknown_width_columns` として別に持つようにした
  （従来は落ちて、消費側が既定幅8.43で埋めていた）

---

## 3. 私が間違えたこと（次の回が繰り返さないために）

**この回の主な成果は、測定と要件の訂正である。** 順に書く。

### 3-1. 死んだ経路を測って「YakuLingoの短縮」と呼んだ

`Invoke-YakuTextShorten` へ入る HTTP 経路は無い。`Start-YakuTranslationJob` の
呼び出しは `Server.ps1` の6箇所すべて `-Kind 'cat'` である。
CAT画面の「情報を保って短くする候補」は `publication-candidates` を押す。

**「YakuLingo 短縮 4/20・倍率2.63」は出荷版の数字ではない。**
同じ理由で `BriefStyle.ps1` の略語表も出荷版では発火しない（4か所のうち3か所が
死んだ経路、残る1か所は常に `-Style 'full'`）。

### 3-2. 利用者の目安を要件へ格上げした

「2倍に収めたい」を合格条件に据えた。利用者の訂正:

> 散文の2.0は自分が適当に書いた目安なので基準にはしない方が良い

**CLAUDE.md「自分が書いた文を、翌日『要件』として引用しない」の、利用者版である。**

### 3-3. 判定の物差しそのものが違っていた

> セルの右側を空白セルにしてそこにはみ出して散文を書くことが多くあるので、
> そういった時に元セルの列幅をひろげるとおかしなことになる。
> **セルに収まっているかよりも、pdfで見て文字が切れていないかが重要**

「原文字数×2の枠に収まるか」は**私が仮に置いた代用**であって要件ではない。
題材の「4/20」「11/20」は**経路の比較には使えるが、合否には使えない。**

### 3-4. 照合器を何度も壊した

```
全角数字を [0-9] で探していた            → 公表訳が 3/7 という嘘
３月 が March になるのを欠落と数えた      → 8/16 という嘘
"March 31, 2026" を "312026" に繋げた     → 月の規則が自己検査で落ちた
YakuLingo の "hundred million" を読めない → 正解3件を赤にした
DeepL の "309,000 units" を千台と読めない → 危うく同じことをするところだった
```

**いずれも「公表訳の点が低すぎる／両者が並びすぎ」という、あり得ない結果が合図だった。**
直すたびに自己検査を先に置き、落ちたら数字を出さずに止める形にしてある。

### 3-5. 突然変異の戻しで cat.js の BOM を落とした

`ReadAllText` は BOM を剥がす。`UTF8Encoding($false)` で書き戻すと消える。
**符号化の門が捕まえた。** 戻すときは `UTF8Encoding($true)`。

---

## 4. 測った数字（引くときはこの断りごと引く）

### 散文20文（公表英文が参照訳）

| | 距離（金額を伏せて） | 倍率 | 数字 |
| --- | --- | --- | --- |
| 素のLLM | 0.547 | 2.75 | 69/69 |
| Smartcat（同梱の無料AI） | 0.525 | 2.50 | 65/69 |
| DeepL（無料版） | 0.515 | 2.89 | 69/69 |
| Nani翻訳（無料版） | 0.483 | 3.05 | 68/69 |
| **YakuLingo** | 0.478 | 2.90 | **69/69** |
| （公表訳＝人間） | — | 2.78 | — |

**素のLLMだけが雑音の外**（対の差 t=3.84、95%区間 0.034..0.105、16/20勝）。
DeepL・Nani との差は20文では区別できない（区間が0をまたぐ）。

### 表のラベル20行

| | 枠に収まる | 倍率 |
| --- | --- | --- |
| 素のLLM（幅を伝えた） | **11/20** | 2.14 |
| Nani翻訳 | 6/20 | 2.78 |
| 素のLLM（幅を伝えない） | 5/20 | 2.73 |
| 公表訳（人間） | 5/20 | 3.21 |
| YakuLingo 通常訳 | 4/20 | 2.88 |
| YakuLingo 短縮（**死んだ経路**） | 4/20 | 2.63 |

同じモデルで幅の指示だけを足すと 5/20 → 11/20。McNemar 正確検定 p=0.031。
**差を担ぐのは6行だけで薄い。** 設計はこの1点に乗っている。

**外部3つはすべて無料版。** 有料版で上がる可能性はあるが、上がる方向は
YakuLingo に不利な側なので結論は覆らない。

---

## 5. 利用者の判断（2026-08-17、決着済み）

| | 回答 |
| --- | --- |
| レイアウト由来の整数をプロンプトへ出す | **可** |
| 出力ファイルの体裁をアプリが触る | **可** |
| 表の項目名を配布TMへ入れる | **可**（判定規則は未定。案は「文でない名詞・複合名詞」「文字数」） |
| 短くした訳をTMへ入れる | **可** |
| 散文の2.0を門にする | **不可**（目安であって基準ではない） |

**作らないと決めたもの。**

- **結合の自動化。** 右がどこまで空白かは機械に判断できない
- **列幅を広げること。** 右へはみ出させるのは意図した書き方
- 固定の略語表を大きくすること（用意していないものは生み出せない、という指摘）

> とにかく見た目重視の体裁になっているのが根本原因なので、むしろそっちを変えたいが、
> 自分の力でできることではないので、あくまで対症療法でアプリやcopilotの判断で
> できそうなことだけ実装して、後は利用者が目で見てどうにかするしかない。

---

## 6. 次にやること

### 6-1. すぐできる（Copilotも実ファイルも利用者判断も不要）

**幅の数をモデルへ届ける。** いま届いていないことは実行で確かめた。

```
cat.js:2603        maxChars = max(20, floor(現訳の長さ × 0.8))   ← セル幅ではない
Publication.ps1:95 ProtectedSidecar = $mask.Text（payload 丸ごとマスク）
→ "max_chars":"40" が "[[N21]]" として出る（2026-08-17 実行で確認）
```

`Assert-YakuNumericPromptProtected` は**3桁未満を検査から飛ばす**
（`Translation.ps1:743-744`）ので、**2桁（99以下）の予算をテンプレートの固定文へ
出す**なら、マスクの実装を1行も変えずに届く。詳細は
`_docs/決定_セルに収める_2026-08-17.md` §3。

### 6-2. 実ファイルが要る（社外秘のため保留）

段階0の残りは「サーバの幅判定と、実際にPDFで切れている箇所が一致するか」。
**利用者から「社外秘なのでちょっと難しいかも」との回答。**
構造だけ同じダミー（列幅と原文の文字数を保った差し替え）でも判定できる。

### 6-3. 利用者に投げてある問い（未決）

**「利用者が略語を登録する仕組みは要るのか。市販ツールにあるか。」**

調べた結果は次のとおり。**この回では結論を出していない。**

- **市販ツールに、略語専用の登録簿は無い。** memoQ の termbase の項目は
  ID / Note / Project / Domain / Client / Subject / Definition / Matching /
  Case sensitivity / Forbidden / Example / POS / Gender / Number で、
  **略語は普通の用語（同義語）として入れ、`Case sensitivity` を Yes にする**
  （「TBD や XML のような略語に良い」と明記）。専用の記録型は無い
  （出典 https://docs.memoq.com/current/en/Concepts/concepts-term-bases-inside-an-entry.html ）
- **アプリ内でも、`AbbreviationEntries` の利用先は `Publication.ps1` だけ**である。
  翻訳・QC・用語集・書き出しは一切見ていない
- **用語集の `cell_exact` が既に同じ仕事をしている。** 註は
  「用語の一貫性ではなく**列からはみ出さないこと**の保証」と書いている
  （`CatProject.ps1:826`）
- **決定的な点。** 数字を含む略語（`上期→1H`、`2026年度→FY26`）は
  `numeric-value-extra`（error）で弾かれ、免除は
  **「利用者が登録した完全一致訳をアプリが一字一句写した場合」だけ**である
  （`CatProject.ps1:668-691`）。つまり**いちばん効く略語は、用語集経由でしか
  通せない。略語登録簿では通せない。**

**私の見立て**: 略語登録簿は用語集と重複しており、しかも本命の略語を通せない。
用語集へ畳むのが筋である。ただし `first_use_rule`（初出で綴る）だけは用語集に
無い概念なので、畳むならその扱いを決める必要がある。**利用者の判断を待つ。**

---

## 7. 測定の道具について（repo には入っていない）

この回の採点スクリプトは `%TEMP%\claude\...\scratchpad\` にあり、**セッションが
終われば消える。repo には1本も入っていない。**

**repo へ移そうとして失敗した。そのことを書いておく。**
`tools/Measure-YakuTranslationComparison.ps1` として1本にまとめ直したが、
自己検査（10件）は通るのに、題材を読む段で
`ArgumentNullException: Value cannot be null. Parameter name: argument` が出た。
落ちる位置は「散文の1行目、`reference_en` の倍率を出した直後」まで絞れたが、
そこから先を突き止められなかった。**動かない道具を入れるほうが害なので、
入れずに消した。** 次に作るときは、この症状から始めれば時間を節約できる。

### 数字は消えない。題材に入っている

**採点はいつでもやり直せる。** 題材の JSON に5経路ぶんの訳文が書き戻してある。

```
_docs/比較題材_Smartcat通し比較_2026-08-17.json
  yakulingo_en / smartcat_en / deepl_en / nani_en / llm_en / reference_en
_docs/比較題材_圧縮された原文_2026-08-17.json
  yakulingo_en / yakulingo_short_en / llm_width_en / llm_nowidth_en / nani_en
  reference_en / source_width
```

### 採点の規則（作り直すときはこの通りに）

```
距離   Get-YakuTranslationMemoryScorer の Score を、両側とも
       ConvertTo-YakuTranslationMemoryKey へ通してから当てる。
       金額表記の設定差を消すため、先に金額を AMT / 数値を NUM へ伏せる
倍率   英字数 ÷ 日本語文字数。日本語は空白を除いて数える
数字   原文の金額（億/兆）・％・千台・年号を値として取り出し、訳文に在るか見る
```

**照合器は必ず自己検査を先に置くこと。** この回は5回続けて嘘をついた。

```
全角数字を [0-9] で探す               → 第１５９期 の 159 を見落とす
３月 が March になるのを欠落と数える   → 公表訳が不当に低く出る
桁区切りを外す処理が日付を壊す         → "March 31, 2026" が "312026"
hundred million を読めない            → YakuLingo の正解を赤にする
309,000 units を千台と読めない        → DeepL の正解を赤にする
```

**合図はいつも「公表訳の点が低すぎる」「両者が並びすぎ」だった。**
参照訳は正解なのだから、低く出たら疑うのは照合器のほうである。

### 消える道具の一覧（作り直す優先順）

```
1  Measure-PromptSize.ps1    組み上がったプロンプトの実測（雛形の足し算ではない）
2  Run-YakuCompressed.ps1    20行を YakuLingo へ通す（Copilot を使う）
3  Probe-Timers.ps1 ほか      Copilot タブの凍結の切り分け（回帰は 9183 にある）
```

`Repro-AbbrRoundTrip.ps1` は**回帰試験として repo へ入れた**
（`tools/Test-YakuV9186AbbreviationRoundTrip.ps1`、コミット `7be3e4f`）。
**送った sidecar から値を読んで応答を組む**ので、モデルにできることと同じことしか
しない。既存の `Test-YakuV9170Publication` が素の `entry_id` で組んでいて欠陥を
見つけられなかった、その穴を塞いである。突然変異2種（マスク鍵の表を落とす／
モデルの復唱を記録する）で、それぞれ赤になることを確認済み。
