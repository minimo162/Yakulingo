# エージェンティック翻訳の前提計測（2026-08-25）

## 結論

- 判定ケース: 欠陥8件を2件捕捉、正常4件の誤検知0件
- 捕捉率: 25%、誤検知率: 0%
- 実Copilot並列度の床: 4ワーカー
- 表示強度: 「照合済み」は使わず「機械とAIの点検を通過」とする

これは12件の小規模な固定ケースであり、一般的な精度保証ではない。再計測は
`app/tools/Measure-YakuAgenticPrerequisites.ps1` を実Copilot環境で実行する。
生の結果はローカルruntimeの
`agentic-prerequisites-20260825-073250.json` と
`copilot-concurrency-20260825-073250.json` にある。

## 判定ケース

欠陥は、省略、否定、範囲、条件、因果関係、程度、数値欠落、数値token不一致の8種。
正常は、単純な事実、条件、範囲、意味を保った簡潔化の4件。意味判定は、訳文だけから
日本語の意味を逆構成する役と、原文との差だけを判定する役に分離した。判定役へ英訳を
見せない条件では、省略・否定・範囲・条件・因果関係・程度の6件を捕捉できなかった。
捕捉できた2件はいずれもローカルの数値token検査による。このため意味照合を精度保証とは
扱わず、表示強度を落とした。

## 並列計測

| ワーカー | 成功 | rate limit | wall秒 | 要求秒合計 | overlap |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 1 | 4/4 | 0 | 24.20 | 23.57 | 0.97 |
| 2 | 4/4 | 0 | 16.06 | 27.07 | 1.69 |
| 4 | 4/4 | 0 | 10.26 | 28.96 | 2.82 |

## 役割分担

| 軸 | 実行主体 | 文脈 | 外部送信 |
| --- | --- | --- | --- |
| 短さ | ローカル | 文字列と選択モード | なし |
| 意味 | 逆構成役 + 差分判定役 | 逆構成役は原文を見ない | 数値マスク済み |
| 読みやすさ | 独立したAI点検役 | 訳文だけ | 数値マスク済み |
| 数値 | ローカル | token整合 | なし |

意味と読みやすさは別Copilotページで並列実行する。ローカル2軸はその待ち時間中に完了する。

## #169 改修後のA/B計測

判定条件を独立して切り替えられるようにした。既定は、判定役へ数値マスク済み英訳を見せ、
1件ずつ、clause対応表を列挙してから判定する条件である。従来条件は次で再現できる。

```powershell
.\tools\Measure-YakuAgenticPrerequisites.ps1 -SkipConcurrency -JudgeEvidence blind -JudgeBatchSize 3 -JudgeOutput binary
```

各軸は同じ固定12ケースで個別に比較する。

```powershell
.\tools\Measure-YakuAgenticPrerequisites.ps1 -SkipConcurrency -JudgeEvidence translation -JudgeBatchSize 3 -JudgeOutput binary
.\tools\Measure-YakuAgenticPrerequisites.ps1 -SkipConcurrency -JudgeEvidence blind -JudgeBatchSize 1 -JudgeOutput binary
.\tools\Measure-YakuAgenticPrerequisites.ps1 -SkipConcurrency -JudgeEvidence blind -JudgeBatchSize 3 -JudgeOutput clause_map
.\tools\Measure-YakuAgenticPrerequisites.ps1 -SkipConcurrency
```

結果JSONには `judge_condition` を記録する。実Copilotを使う再計測はWindows実機で行う必要が
あるため、この変更では未実測の数値を追記しない。catch rate 90%以上かつ誤検知率10%以下を
実測するまでは、表示強度「機械とAIの点検を通過」を維持する。
