# V91.59 実装記録

- ベース: V91.58
- Paths.ps1: `Get-YakuDataDir` に `YAKULINGO_DATA_DIR` 退避先指定を追加。
  `Get-YakuUserSettingsPath` と `Get-YakuLegacyUserSettingsPath` を追加。
- Settings.ps1: `Resolve-YakuUserSettingsPath` を追加し、`Read-YakuSettings` /
  `Read-YakuUserSettingsStrict` / `Save-YakuUserSettings` の保存先を
  `%USERPROFILE%\.yakulingo-ps\config\user_settings.json` へ変更。
  旧設定は初回読込時に1回だけ複製し、旧ファイルは書き換えも削除もしない。
- tools/Test-YakuV9159SettingsPath.ps1: 新規追加（4ケース17アサーション）。
- tools/Smoke-Test.ps1: 設定テストを `YAKULINGO_DATA_DIR` で隔離し、
  保存先・引き継ぎ・旧ファイル不変を検証するアサーションを5件追加。
- tools/New-YakuPackage.ps1: manifest.json（schema/version/build_id/generated_at/
  file_count/total_bytes/files[path,size,sha256]）の生成を追加。相対パスの序数順で固定。
  user_settings* / *.bak / *.tmp / ~$* を含むツリーの packaging を拒否。
- tools/Test-YakuPackage.ps1: manifest とZIPの突合を追加（build ID一致、
  各ファイルのサイズとSHA-256、未記載ファイルの不在、file_count一致、禁止ファイルの不在）。
- bootstrap.ps1（共有ルート・新規）: current.txt解決、manifest検証付きローカル複製、
  ハッシュ入り配置先名、リネーム公開、旧版掃除、オフライン継続、旧版互換。
- YakuLingo起動.cmd（共有ルート・新規、ASCIIのみ）: bootstrap.ps1 をローカルへ複製して実行。
- YakuLingo起動.vbs（共有ルート）: .cmd へ転送する互換シムに置換（VBScript廃止予定のため）。
- V91.59/YakuLingo起動.cmd（新規）: 保守用にその場で直接起動。
- tools/Create-Desktop-Shortcut.ps1: 共有ルートの .cmd を指すよう変更。
  YAKULINGO_SHARED_ROOT を自動利用し、作業フォルダはローカルパス（UNC警告回避）。
- tools/Test-YakuBootstrap.ps1: 新規追加（7ケース15アサーション）。
- tools/New-YakuPackage.ps1: 隠しファイルを列挙対象に含め（-Force）、ZIPを
  Compress-Archive ではなく manifest の一覧から直接組み立てるよう変更。
  Compress-Archive は隠しファイルの扱いがプラットフォームで異なり、
  ZIPとmanifestが食い違うため（bootstrap の回帰テストで app/.vscode/settings.json の
  取りこぼしとして検出）。
- New-YakuUploadFolder.ps1（共有ルート・新規）: 作業ツリーを変更せずアップロード用
  フォルダを作成。ルート直下は許可リスト、版フォルダは除外リスト、保持は現行版とN-1、
  複製後に manifest を再生成、仕上げに除外漏れ検査。
- アップロード用フォルダ作成.cmd（共有ルート・新規、ASCIIのみ）: 引数なしでデスクトップ、
  フォルダのドラッグ＆ドロップでその中に作成。
- tools/New-YakuPackage.ps1: -ManifestOnly を追加（manifest のみ再生成、OutputPath任意）。
- 共有フォルダ配置手順.md: 起動方式の節を追加し、配置手順・想定構成・N-1の説明を更新。
  手順Aの先頭にアップロード用フォルダ作成を追加。
- app/README.md: 設定の保存先を更新し、V91.59節を追加。
- app/config/build.txt: V91.59へ更新。
- current.txt: V91.59へ更新。
- N-1構成: V91.58を保持し、N-2のV91.57を削除。
- 検証: PowerShell 7.4.6（Linux）で全 `.ps1` の構文解析とUTF-8 BOM検査を実施し違反0件。
  `Test-YakuV9159SettingsPath.ps1` は17アサーション全通過。
  `Smoke-Test.ps1` は設定ブロック（新規5件を含む）を通過。
  packaging は実ツリーで通しで実行し、58ファイル・1,368,483バイトの manifest 生成と
  `Test-YakuPackage.ps1` の PASS を確認。2回生成して generated_at 以外の完全一致を確認。
  異常系5件（1バイト改ざん / manifest未記載ファイル / manifest欠落 /
  user_settings混入 / build ID不一致）がいずれも所定のエラーで停止することを確認。
  `Test-YakuBootstrap.ps1` は7ケース15アサーション全通過（初回導入・再利用・更新と掃除・
  オフライン継続・共有もローカルも無い場合の停止・改ざん検出・旧版互換・不正ポインタ）。
  アップロード用フォルダ作成は汚した作業ツリーを模擬して8ケース43アサーション全通過
  （作業ツリー不変・許可リスト・保持世代・除外・manifest再生成・出力物からのbootstrap導入・
  安全側の停止4種・-Forceでの作り直し）。
- 未検証: `.cmd` と `.vbs` シムはWindows実機で未実行（本環境にcmd.exe/WSHがないため）。
  bootstrap.ps1 本体はPowerShell 7で全経路を検証済み。
- 未検証: `Smoke-Test.ps1` は line 144 の
  `Parse-YakuTextTranslationResponse` インラインマーカー検証で
  `RESPONSE_END_MARKER_MISSING` により停止する。V91.58でも同一箇所・同一エラーで
  停止するため本版の変更に起因しない。本ツールの対象は Windows PowerShell 5.1 であり、
  PowerShell 7 / Linux 環境差と判断。Windows実機での全体通し確認は未実施。
