# YakuLingo 実機テスト記録（中間管理職ペルソナ）

環境: 実機。`YakuLingo起動.cmd` から起動。バックエンド `127.0.0.1:8765`、Copilot warmup ready。
制約: 画面操作の許可が下りなかったため、デスクトップシェルの見た目は未確認。
同じサーバをブラウザで開いて検証している（HTML・JS・API・Copilot はすべて実物）。

---

## シチュエーション1: 部下からのメールを見て、初めて起動する

### 起動
- `YakuLingo起動.cmd` → `bootstrap.ps1` → シェル(YakuLingo.exe) + バックエンド + Copilot用Edge が起動。
- Copilot は `Copilot warmup ready from fresh-chat after state`。約8秒でready。
- ポート9433が埋まっていたため9434へ自動切替。ログに日本語で記録あり。**問題なし**。

### 画面（初回利用者はチュートリアルへ）
4画面すべての本文を確認した。

| 画面 | 見出し | 内容 |
|---|---|---|
| 1/4 | YakuLingoでできること | 数文なら「ちょっと翻訳」。WordやExcelなどの資料は「資料翻訳」 |
| 2/4 | ちょっと翻訳をすぐ開く | Ctrl+Alt+J |
| 3/4 | 送信される内容 | 金額などの数値は伏せる。社名・人名はそのまま |
| 4/4 | 次回からすぐ使う | 自動起動とショートカットの設定 |

### 判定: 不合格（チェックポイント「どんなアプリかすぐに分かるか」）

**4画面のどこにも、このアプリが「何語から何語へ訳すのか」が書かれていない。**
「翻訳」とだけあり、日→英なのか英→日なのか、両方なのかが分からない。

**翻訳エンジンが Microsoft 365 Copilot であることも、3画面目で初めて、
しかも「Copilotへ送る」という送信先としてしか出てこない。**

部下からメールをもらった上司の関心は「これは何のアプリで、何を使っていて、
自分の仕事に関係あるか」である。1画面目がそれに答えていない。
見出し「YakuLingoでできること」は、YakuLingo が何かを既に知っている人の書き方。

合格した点:
- 開始画面の h1「何を訳しますか」と2択は、次にやることが明確
- 右上「使えます」で準備状態が分かる
- 「金額などの数値は…伏せます」が下部に常設

### 併せて見つけた欠陥（軽微ではなかった）

シェルの実際のホーム窓（1240x800）で実測した。

| 状態 | 主要カードの下端 | はみ出し |
|---|---:|---:|
| 続きから3件・標準文字 | 800 | 0（ぎりぎり） |
| 続きから1件・標準文字 | 636 | 0 |
| **続きから3件・「文字を大きく」** | **903** | **103px** |

**「文字を大きく」を押すと、主要な2つの入口が画面外へ出る。**
老眼のペルソナがまさに押すボタンで、押した結果いちばん大事なものが見えなくなる。
原因は今回私が足した「続きから」（最大3件）。

### 修正（実施済み・再測定で確認）
1. チュートリアル1画面目を書き換え。「日本語と英語を訳すアプリです」＋
   「Microsoft 365 Copilotへ文章を送り、訳案（下書きの訳）を受け取ります。
   そのまま使える訳ではありませんので、内容はご自身で確かめてください。」
   開始画面の説明文にも「日本語と英語を、Microsoft 365 Copilotで訳します。」を入れた。
   → Copilot が「データの送信先」として初出していた順序を、「訳す道具」が先に来る形へ直した。
2. 「続きから」を直近1件に絞り、残りは「ほかに保存した作業が N 件あります」のリンクへ。
   → 文字を大きくしても下端786/800。はみ出し0。

---

## シチュエーション2: 外国人上司の英文メールを和訳（合格）

561字のメール（3つの質問・12.4%・火曜・金曜・3月・子会社2社）を貼って「訳案を作る」。

- 訳出は正確。数値・曜日・月・件数すべて保持。「please tell me now rather than later」も
  「後になってからではなく、今の時点でお知らせください」と含意まで訳せている
- 方向は自動判定で英→日。見出しは「日本語の訳案（内容確認用）」
- 使い方は迷わない。貼る→ボタン1つ

## シチュエーション3: 日本語の返信を英訳（合格）

- 12.4%増・8ポイント・4ポイント・子会社2社・金曜日中・火曜日の取締役会、すべて保持
- 所要2秒。見出し「英語の訳案（未確認）」、注記「外部へ配布する資料に使う場合は…」
- 「この訳を資料翻訳へ移す」が英訳のときだけ出る（和訳では非表示）

## シチュエーション4: 表現の一部を直す（合格）

- 訳文のすぐ下に「この訳案を直す」。押すと入力欄と例（プレースホルダ）が出る
- 「『Thank you for contacting us.』を『Thank you for your message.』に変えてください。」
  → その1箇所だけが変わり、**他の文と全数値は不変**。所要3秒
- 待機中「いまの訳案は、終わるまで変わりません。このまま待つだけで大丈夫です。」

軽微: 完了時の状態表示だけ「AIによる未確認の訳案です」と `AI` を使っている。
他の画面は `Copilot` で統一しているので揺れている。

## シチュエーション5: 未公開の数字を伏せて英訳

「来期の営業利益計画は152億円、売上高計画は2,340億円です。」を英訳。

- 訳文: `152 oku` / `2,340 oku`。**oku 表記の単位規約が効いている**（シチュエーション6の論点も先取り）
- ログで実際のマスクを確認: `Numeric masking. location=text masked=8 kept=0` →
  `protected-receipt-final masked=0 kept=8`。送信時に伏せ、受信後に戻している
- 画面表示: 「2件の数値を伏せてCopilotへ送り、訳案では元の数値に戻しました。」

### 判定: 部分的に不足

マスクは**確実に効いている**。しかし画面が出すのは**件数だけ**で、
「自分のあの数字（152億・2,340億）が伏せられたか」は確認できない。
1件取りこぼしても利用者は気づけない。
「この数字は未公開だから隠したい」という動機で使う人には、件数は答えになっていない。

### 修正（実施済み）

「伏せた数値を確かめる」を折りたたみで追加し、伏せた実値を `[[N1]] 152億` の形で並べる。
6件までは開いた状態、それ以上は畳む。

**実装上の判断**: 対応表(Map)を結果オブジェクトへ運ぶ案は採らなかった。
`Translation.ps1:1744-1745` に「対応表(Map)は結果オブジェクトへ載せない(§8)」という契約があり、
quick 以外の経路の結果まで汚すため。代わりに画面へ渡す直前に**原文から作り直す**。
これは製品自身が再依頼経路（`Translation.ps1:1789-1794`「割り当ては原文だけで決まる」）で
既に依存している前提であり、新しい仮定を持ち込まない。

クライアント側で数値を検出する案は捨てた。漢数字・英語綴り数・保護スパンをJSで再現できず、
照合が本体とずれる。「152億円は伏せました」という嘘を出す危険があり、件数だけの現状より悪い。

**安全弁**: 作り直した件数が保存済みの件数と食い違ったら値を出さず、件数表示へ落とす。

安全性の確認:
- QuickArtifact はメモリのみ・TTL30分。ディスクへ書かないことは既存検査が固定している
- quick経路は `diagnostics_level` を強制的に standard へ落とすので、実値はログに出ない
- `/api/quick/jobs/{id}` の応答は元から `source_text`/`translation` を実値で返しており、露出面は増えない

### 再テスト（合格）
`[[N1]] 152` `[[N2]] 2,340` が最初から開いた状態で並ぶ。値は17px。
注記「この一覧はこのパソコンの中だけで作っています。Copilotへは記号で送りました。」

---

## シチュエーション6: 社内資料を丸ごと英訳（資料翻訳）

社内資料8行（売上高3,120億円・営業利益152億円・前年138億円・8.2%・10.1%・24億円・18.5%・3.4%）を
「長い文章を貼り付ける」から取り込み、「残りの訳案を作る」を実行。

### 合格した点

**進捗表示・キャンセル・完了通知が、今回の実装どおり実機で動いた。**

| 経過 | 表示 |
|---|---|
| 1秒 | 翻訳中 10% ／ Copilotの返事を待っています ／ 0秒経過 ／ **翻訳をやめる** |
| 2秒 | Copilotへ送っています 28% ／ 入力 421字 |
| 4秒 | Copilotの返事を待っています 39% |
| 6秒 | 訳文を受け取っています 43% ／ Copilotが訳文を書いています（0字ぶん受け取りました。全体はおよそ716字です） |
| 10秒 | 69% ／ 520字ぶん受け取りました |
| 完了 | タスクバーが「✔ 翻訳が終わりました」 |

- 「翻訳をやめる」は全サンプルで存在した（以前は1秒で消えていた）
- タスクバーのタイトルが % を追う
- `oku` 表記が効いている（`3,120 oku` `24 oku`）
- 訳質は良い。「在庫調整の影響により」「期初計画を据え置きます」なども自然

軽微: 1バッチしかないのに「翻訳中（1/1 回目）」と出る → 修正済み（total>1 のときだけ付ける）

### 不合格: 2行が訳文なしで戻る

8行中2行が空。ログ:

```
File batch parsed. expected=8 received=8 missingIds= reason=normal
[WARN] Numeric mask integrity. cat-ID-1 missing=0 duplicated=0 unexpected=0 outOfOrder=True [order=[[N3]],[[N2]],[[N1]]]
[WARN] Numeric mask integrity. cat-ID-3 missing=0 duplicated=0 unexpected=0 outOfOrder=True [order=[[N1]],[[N3]],[[N2]]]
```

**Copilot は全8行返している。アプリが2行を捨てた。**
`Translation.ps1:927` が数値の並び順違いを不合格とし、`CatProject.ps1:2029` が
`$Map.Remove($idx); continue` で訳文ごと破棄している。

捨てられた2行は、いずれも**英語として正しい語順**:
- 「2027年3月期 第1四半期」→ "first quarter of the fiscal year ending March 2027"（2027/3/1 が入れ替わる）
- 「152億円で、前年同期の138億円から10.1%の増益」→ "up 10.1% from 138 oku"（138 と 10.1 が入れ替わる）

`missing=0 duplicated=0 unexpected=0` なので、原文の数値は過不足なく1回ずつ入っている。
日英で語順が変わるのは当然で、決算文では頻出する。

利用者への伝わり方は最低限ある（「訳あり6行・残り2行」「未翻訳 2」「行に未翻訳の印」）が、
**なぜ空なのかは分からない**。もう一度押しても、同じ理由でまた捨てられる。

### 根拠が成立していなかった

`Translation.ps1:904-905` のコメントはこう書いていた:

> 個数が同じでもN1/N2の順序を入れ替えると、売上高と利益など別の勘定へ実値を戻してしまう。

**これは事実と違う。** `Restore-YakuNumericMask`（`Translation.ps1:861-876`）は
`$result.Replace($token, $replacement)` でトークン名で置換する。
`[[N2]]` は文中のどこにあっても N2 の実値に戻る。順序が変わっても実値は取り違えない。

順序検査が実際に見ているのは「モデルが勘定と数値の対応を取り違えたかもしれない」という
意味の疑いであって、復元の安全ではない。そして自然な語順変更と取り違えを、
トークン列だけでは区別できない。**訳文を捨てる根拠が無い。**

### 修正（実施済み）

1. `Translation.ps1:927` の `Ok` から `-not $outOfOrder` を外した。
   `Ok` は「実値へ戻して安全か」だけを表す述語に戻した。`OutOfOrder` は戻り値に残る
2. `CatProject.ps1` の破棄を原因別にした。捨てるのは
   missing / duplicated / unexpected のときだけ（§8「数値が抜けた訳は欠陥」どおり）。
   順序だけなら訳文を残し、「並び順が原文と違います。訳文は取り込みました」と警告する
3. 会計期の除外規則を `Get-YakuFiscalPeriodMaskTokens` に集約し、
   `年度第◯四半期` に加えて `◯年◯月期（第◯四半期）` も扱う。
   従来は Translation.ps1 と CatProject.ps1 に別実装があり、**両方が同じ漏れ**を持っていた
   （§9「検査が、閉じようとした穴と同じ形の穴を持っていた」の再来）
4. `Test-YakuV9162Horizon1.ps1` の該当検査を新しい契約へ更新

**安全性**: 疑わしい行を止める網は残っている。確認時QCの `numeric-value-order-mismatch` が
`Severity='error'` で確定を止め、出力は確認済みの行しか通らない。
違いは「訳文が消える」から「訳文は残るが確定できない」になったこと。人が直せる。

### 再テスト（合格）

同じ資料を流し直した。

```
cat-ID-3  missing=0 duplicated=0 unexpected=0 outOfOrder=True  → 訳文を保持（以前はここで破棄）
cat-ID-1  missing=[[N3]]                                        → 破棄（数値が実際に欠落。正しい）
cat-ID-2  missing=[[N1]]                                        → 破棄（同上）
```

もう一度「残りの訳案を作る」を押すと全4行が埋まった（「訳あり4行・残り0行」）。
**行き止まりにならない**ことを確認。

### さらに1件: 「短くする」で画面が壊れる（修正済み）

3行目で「短くする」を押すと、画面に生のエラーが出た:

> Cannot read properties of null (reading 'source')

スタックを取ると `cat.js:240` の `terminology_generation.map` で、配列に **null 要素**が
混じっていた。訳文グリッド全体の描画が落ちる。サーバ側の修正自体は成功しており、
壊れていたのは描画だけだった。

修正: null 要素を除外。あわせて、内部エラー（TypeError 等）を利用者へそのまま
見せないようにした（記録へ回し、画面には「画面を読み込み直してください」と出す）。

再テスト: エラーは消え、比較欄（この案を使う／元に戻す）が正常に出た。
124字 → 53字 `Operating profit: 152 oku, up 10.1% YoY from 138 oku.` 数値は全保持。

### 判定: 合格（3件の修正後）

- 画面の使い方: 取り込む → 残りの訳案を作る → 1文ずつ確認。迷わない
- `oku` 表記: 効いている。なお `billion` は §6 の決定で意図的に無効（換算コードが無く10倍になるため）
- 短く訳す: 効いている

---

## シチュエーション7: 有価証券報告書の3way翻訳

前年度の日本語・前年度の英語・当年度の日本語を貼り付けて開始。

### 合格した点

- **3wayの分類が正しい**: 前回と同じ2／変更あり1／今回追加1
- **候補ペインに前回訳が出所付きで出る**:
  「前回の資料の訳／有価証券報告書_2027年3月期（前回版）／前回版 段落 1」
  `Ctrl+1` の案内つき、「この訳を挿入」で挿入できる
- **挿入の履歴が残る**: 「この参考訳から挿入（その後編集なし）・…・前回版 段落 1」
  「使った参考訳を確認」で原文・訳文も見られる
- 自動では反映しない（§0の約束どおり）。挿入は利用者の明示操作
- 1兆3,150億円 → `13,150 oku` の桁換算は正しい
- **「確認済みの行だけコピー」が機能する**:
  「全 4 行のうち、確認済みの 1 行だけをコピーしました。まだ確認していない 3 行は入っていません。」

コーパスからの参考訳は、この内容が取り込み済みコーパスに無いため提示されなかった。
§4-3 のとおり pairs が配布物に入らない問題は別途残っている。

### 未解決の欠陥: アプリ自身の単位変換を、アプリ自身のQCが拒否する

原文「当連結会計年度の売上高は1兆3,150億円となりました。」
訳文「Net sales for the current consolidated fiscal year were 13,150 oku.」（換算は正しい）

確認済みにできない。QCの指摘:
> 原文にある数字が、訳文で違う値になっているか、抜けています。
> 原文に無い数字が訳文に入っています。余分な数字を消してください。

原文から `1`（兆）と `3,150` を、訳文から `13,150` を抽出して突き合わせているため。
**1兆単位の金額を含む行は、確認済みにできず出力に載せられない。**
有価証券報告書・決算短信では頻出する。

これは今回直した「登録した略語（1H・Q1）をQCが拒否する」と**まったく同じ型**である。
ロードマップ §4-1 が「CATだけ単位変換を通らない」として既に開いている領域でもある。

今回は手を付けていない。数値QCに二度目の手を入れることになり、
順序の件と同じだけの検証（どの型を許し、どの型を止めるか）が要るため。
当面の回避策は「確認済みの行だけコピー」で、該当行を外して持ち帰れる。


---

## 追記: 画面を目視して見つけたもの（2026-08-11）

利用者の許可を得て、デスクトップシェルの画面を実際に見た。
それまではブラウザで同じサーバを開いて検証しており、シェル固有の見え方は未確認だった。

### 目視で確認できた（合格）

- 開始画面に「日本語と英語を、Microsoft 365 Copilotで訳します。」が出ている
- 「続きから」は1件＋「ほかに保存した作業が 9 件あります」。**「文字を大きく」を押しても
  主要な2つの入口が両方とも画面内に収まる**（修正前は103pxはみ出していた）
- トレイの両押しで**資料翻訳の画面がそのまま復帰する**（修正前は「ちょっと翻訳」へ飛んでいた）
- `3,120 oku` → `3,120億円` の逆方向の換算も正しい

### 目視でしか出なかった欠陥（修正済み）

**1. 「場所」列で日本語が1文字ずつ縦に折り返される**

ボタン名を「次の行と結合」→「次の行とつなげて1文にする」と長くした際、
116px の場所列に入ることを確認していなかった。実機では縦一列になって読めない。
→ 繋ぎ直しのボタンを幅のある訳文セル側へ移し、ボタン内は改行させないようにした。
検査も「CSSのvisibilityで隠す」という字面から「開いている行にだけ描く」という保証へ変えた。

**2. 和訳の作業では「短くする」も「この指示で直す」も一切使えない**

画面に PowerShell の生エラーが出た:
> パラメーター 'Mode の引数を確認できません。引数 "" は、ValidateSet 属性で指定された
> セット "full,brief" に属していません。

`Translation.ps1` が `$mode = if ($Direction -eq 'to_en') { $Style } else { '' }` としており、
和訳で空文字を渡して ValidateSet に弾かれていた。
`Get-YakuTextRequiredLabels` は to_jp では Mode を見ずに JAPANESE_TEXT を返すので、
既定の `full` を渡せばよい。→ 修正し、実機で「短くする」が成功することを確認
（`第1四半期の売上高は3,120億円、前年同期比8.2%増。`）。

既存の Revise 検査は**プロンプト生成しか見ておらず**、その先の解析呼び出しを通していなかった。
そこまでは Copilot 往復なしでは守れないので、空 Mode を渡さないことを回帰に足した。

**3. プライバシー表記が既定のウィンドウで折り返しの下にある**

「金額などの数値は、Copilotへ送る前に自動で伏せます。社名・人名・文章はそのまま送信されます。」
が、既定のホーム窓ではスクロールしないと見えない。アプリの安全性の要である。
→ 未対応。ウィンドウ既定サイズか配置の見直しが要るため、別途。

### 確認できなかったもの

**トレイの完了通知**。この端末は Windows の「応答不可」が有効で、
「[応答不可] 設定に基づいて、アラームのバナーのみが表示されます」と通知センターに出ている。
バナーが抑止されるため、目視できなかった（利用者の設定なので変更していない）。

なお、会議中の人は応答不可を入れていることが多い。その場合トレイ通知は届かないが、
**タスクバーのタイトル（`✔ 翻訳が終わりました`）は抑止されない**。
今回の実装で通知手段が二重になっていることには意味がある。

### トレイアイコンについて（未対応）

`SystemIcons.Application`（Windows既定の汎用アイコン）のままで、
他の8個のアイコンと見分けがつかない。しかも既定では隠れたオーバーフロー領域に入る。
チュートリアルは「画面右下の時計のとなりにYakuLingoのアイコンが出ます」と書いており、
実態と食い違っている。専用アイコンを持たせるか、文言を実態へ合わせる必要がある。


---

## 追記2: 通知・アイコン・単位・デザイン（2026-08-11）

### 完了通知（動作を確認）

応答不可をオフにした状態で、資料翻訳の「短くする」を実行して最小化したところ、
右下に出た。

> **YakuLingo** — 翻訳が終わりました。YakuLingoを開くと結果を確認できます。

前回出なかったのは Windows の「応答不可」が有効だったためで、実装の問題ではなかった。
ただし応答不可は戻ることがある（実際に一度戻っていた）ので、設定に左右されない手段を足した。

- `FlashWindowEx` でタスクバーのボタンを点滅させる。通知領域のアイコンが隠れていても、
  応答不可でも効く
- タスクバーのタイトル（`✔ 翻訳が終わりました`）も従来どおり

### アイコン（対応済み）

`SystemIcons.Application`（Windows既定の汎用アイコン）をやめ、専用アイコンを作った。
日本語の画とラテン文字の A を矢印でつないだ意匠。16〜256pxの7サイズを含む。

- `desktop/YakuLingo.ico` を新規作成（依存を増やさないため PNG/ICO を直接組んだ）
- `Build-DesktopShell.ps1` に `/win32icon` を追加。**exe 自体に埋め込む**ので、
  デスクトップとスタートメニューのショートカットにも出る
- 実行時はウィンドウと通知領域が同じアイコンを使う（`LoadAppIcon`）
- 実機で確認: デスクトップ、タイトルバー、タスクバーとも新しいアイコン

### 1兆単位（対応済み・§4-0 を閉じた）

原因は3つ重なっていた。

1. 数値の突き合わせだけが単位変換を通っていなかった（`$audit` は通っていた。4行違い）
2. 変換後の原文は訳文側の表記なのに、日本語向けに分類していた。
   同じ `13,150 oku` が `number|13150` と `currency:yen|1315000000000` になっていた
3. 単位検出が `oku` の後ろに続く日本語を**列挙**していた（です・でした・句読点）。
   「となりました」「に達しました」が漏れる。`k units` も同じ

列挙をやめ「後ろが英数字でなければ単位」という規則にした。
通るべき6件・止まるべき5件で確認（10分の1にした訳、千を落とした訳は従来どおり止まる）。

### デザイン（対応済み）

利用者の指摘「ぎゅうぎゅうで文字が枠に収まっておらず、情報過多で目が泳ぐ」。
測ってみると、主役であるはずの中央（原文と訳文）が **面積の47.7%**しか無く、
脇役2つの合計 50.4% に負けていた。

| | いま | 直した |
|---|---|---|
| 見出しと本文の差 | h3 は本文の1.06倍。しかも `calc(+Npx)` なので**拡大するほど平坦化** | 比率（1.55/1.24/1.10rem）で持つ。拡大しても階層が保たれる |
| 主役の文字 | 本文と同寸 | `--font-segment: 1.12rem` で一段大きく |
| ペイン幅 | 220px / 560px / 380px の固定 | `clamp()` で可変。主役へ幅を回す |
| 余白 | `--space-*` が px 固定、cat-workspace では未使用で生px約20種 | rem 化 |
| いま見ている行 | `tr` の背景が `[data-yaku-confirmed="0"] > td` に負け、**未確認の行では塗りが描かれなかった** | `> td` を塗り、左に6pxの軌 |
| 「場所」列 | `overflow-wrap: anywhere` で日本語が1文字ずつ縦に折り返る | `keep-all` + 省略記号 |
| 行の印 | 「Copilot訳・未確認」が狭い列から溢れ、隣の列と重なっていた | 「未確認」に短縮し、意味は title へ |
| ツールバー | ボタン4つが折り返して136px | 主要2つ＋「そのほか」。116px |
| 右ペイン | 行を選ぶ前から灰色の説明が約9行 | 1行＋畳み |
| CATのウィンドウ | 画面内のカードから入ると `OpenCat()` を通らず、ホームの窓のまま3列が開いていた | 遷移時に一度だけ広げる |
