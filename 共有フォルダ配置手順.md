# V91.37 共有フォルダへの配置・更新手順

## 0. 起動方式（V91.59以降）

利用者は共有ルートの `YakuLingo起動.cmd` から起動します。`bootstrap.ps1` が
`current.txt` の指すバージョンをローカル（`%LOCALAPPDATA%\YakuLingo\versions\`）へ
複製し、`manifest.json` で全ファイルのSHA-256を照合してから起動します。

- 複製先は「バージョン名 + manifestハッシュ8桁」で決まります。同じバージョン名で
  中身を差し替えても別フォルダになるため、**使用中のローカル版を上書きしません**。
- 起動後は共有フォルダを参照しないため、**利用者が作業中でも共有フォルダの
  バージョンを更新できます**。反映は各利用者の次回起動時です。
- 共有フォルダへ到達できないときは、導入済みのローカル版で起動します。
- `manifest.json` を持たない旧パッケージ（V91.58以前）へロールバックした場合は、
  従来どおり共有フォルダ上で直接起動します。
- VBScriptはMicrosoftが廃止予定のため、`YakuLingo起動.vbs` は `.cmd` へ転送するだけの
  互換シムになりました。ショートカットを `.cmd` へ切り替えたら削除できます。

## A. 今回の配置

0. 作業ツリーから **アップロード用フォルダを作成**します。ルートの
   `アップロード用フォルダ作成.cmd` をダブルクリックすると、デスクトップに
   `ECM資料英訳ツール_<版>_<日時>` が作られます。フォルダをドラッグ＆ドロップすると
   その中に作られます。作業ツリーは一切変更されません。
   - 共有ルート直下は許可リスト方式で、必要な構成物だけを複製します。
   - バージョンフォルダからは利用者設定・作業ファイル・OSのゴミ・開発ノートを除きます。
   - 複製後に各バージョンの `manifest.json` を実体に合わせて作り直します。
   - 保持する版は現行版とN-1の2世代です（`-KeepGenerations` で変更可）。
   - 複製しなかったものは理由付きで一覧表示されるので、意図どおりか確認します。
1. ZIPから配置する場合は、共有フォルダ外の一時場所へ完全展開します。
2. 共有ルートへ `V91.37` フォルダをコピーします。コピー途中の状態を利用者に見せないため、可能なら `V91.37_tmp` の名前でコピーし、完了後に `V91.37` へリネームします。
3. ルートの `_docs` を共有ルートの `_docs` へ統合します。既存の `app\docs` 119ファイルは `_docs\ARCHIVE_V13-V91` に1部だけ保持します。
4. ルートの `YakuLingo起動.cmd`、`bootstrap.ps1`、`YakuLingo起動.vbs`（互換シム）を配置し、
   最後に `current.txt` を新バージョンへ更新します。
5. 利用者のショートカットは共有ルートの `YakuLingo起動.cmd` を指す状態にします。
   作業フォルダはローカルパスにします（共有フォルダにすると cmd.exe が UNC の警告を出すため）。
   `app\tools\Create-Desktop-Shortcut.ps1` がこの設定でショートカットを作成します。

## B. docsの扱い

- バージョンフォルダの `app\docs` は配布物に含めません。
- `README.md` と `DESIGN.md` は利用者・保守者向けのため、各バージョンの `app` 直下に残します。
- 開発ノートは共有ルートの `_docs` に集約します。
- 既存119ファイルは `_docs\ARCHIVE_V13-V91` に保持します。
- 今後の修正指示書・開発ノートも `_docs` に追加し、各バージョンへ複製しません。

## C. 通常のバージョン切替とロールバック

- 新版は必ず別フォルダへ展開し、使用中の既存バージョンフォルダを上書きしません。
- 切替は `current.txt` の1行を書き換えるだけです。
- 新版のパッケージ作成時は、`current.txt` と `app/config/build.txt` の両方を新バージョンへ更新します。
  `New-YakuPackage.ps1` が `manifest.json` を生成し、`Test-YakuPackage.ps1` がZIPと突合します。
- ルートの `bootstrap.ps1` はバージョンフォルダの外にあるため、パッケージ更新とは別に配置します。
- 使用中の利用者は旧フォルダを参照したまま処理を完走できます。
- ロールバックは `current.txt` を1世代前のフォルダ名へ戻します。
- `Assert-YakuBuildIdentity` のフェイルファストは緩和しません。

## D. 保持世代（N-1）

共有フォルダには、現行版と1世代前だけを保持します。`archive` フォルダは作成しません。

V91.59以降はローカル複製方式のため、使用中の利用者がいてもバージョンフォルダを
差し替えられます。N-1の保持は「使用中だから消せない」ためではなく、
`current.txt` の書き換えだけでロールバックできる状態を保つためです。

例としてV91.37へ切り替えた場合:

1. 切替当日から翌営業日に、V91.28を一時名へリネームできるか確認します。リネームできない場合は使用中の可能性があるため削除しません。
2. リネームでき、利用中でないことを確認したらV91.28を削除します。
3. 共有フォルダにはV91.37（現行）とV91.35（N-1）だけを残します。
4. それ以前のZIP原本は管理者ローカルに数世代保管し、共有フォルダには置きません。
5. N-2以前へ戻す必要がある場合は、管理者ローカルのZIPを共有ルートへ展開し、`current.txt`を切り替えます。

## E. 想定構成

```
ECM資料英訳ツール/
  YakuLingo起動.cmd        ← 利用者のショートカットはこれを指す
  YakuLingo起動.vbs        ← .cmd へ転送する互換シム（将来削除）
  bootstrap.ps1            ← ローカル複製と検証
  current.txt
  _docs/
    ARCHIVE_V13-V91/
    V91.37_修正指示書_docs分離_N-1保持運用.md
  V91.58/
    YakuLingo起動.vbs
    app/
  V91.59/
    YakuLingo起動.cmd      ← 保守用。共有フォルダ上で直接起動する
    YakuLingo起動.vbs
    manifest.json          ← New-YakuPackage.ps1 が生成
    app/
```

同一バージョンフォルダを使用中に上書きしないでください。
