# 共有フォルダへの配置・更新手順

## 0. 起動方式

利用者は共有ルートの `YakuLingo起動.cmd` から起動します。`bootstrap.ps1` は共有ルート直下の `app/` と `manifest.json` を読み、配布物を `%LOCALAPPDATA%\YakuLingo\versions\<build ID>-<manifestハッシュ>` へ複製します。全ファイルのサイズとSHA-256を照合してから、ローカル側の `app/` を起動します。

- 共有フォルダでは `app/` を直接配置します。`V91.60/`、`V91.61/` のような版名フォルダと `current.txt` は使いません。
- 同じ build ID で中身を更新しても manifest ハッシュが変わるため、ローカルでは別フォルダへ導入されます。使用中のローカル版を上書きしません。
- 起動後は共有フォルダを参照しないため、利用者が作業中でも共有側を更新できます。反映は次回起動時です。
- 共有フォルダへ到達できないときは、完全性を再確認できた導入済みローカル版で起動します。
- 開発ツリーなど `manifest.json` がないフラット配置では、共有ルート直下の `app/` を直接起動します。

## A. アップロード用フォルダの作成

1. ルートの `アップロード用フォルダ作成.cmd` をダブルクリックします。
2. デスクトップに `ECM資料英訳ツール_<build ID>_<日時>` が作成されます。フォルダをドラッグ＆ドロップすると、そのフォルダ内に作成されます。
3. 作成物には次のものだけが入ります。

   ```text
   ECM資料英訳ツール_<build ID>_<日時>/
     YakuLingo起動.cmd
     bootstrap.ps1
     共有フォルダ配置手順.md
     manifest.json
     app/
   ```

4. `New-YakuUploadFolder.ps1` は `app/` から利用者設定、ログ、一時物、開発用依存、評価生成物を除き、ルートの `manifest.json` を実体に合わせて作り直します。
5. `_docs`、`.git`、`node_modules`、会社・資料固有の用語集、固有名詞一覧、コーパスは配布しません。複製しなかったものは理由付きで表示されます。

## B. 共有フォルダの更新

1. 作成したアップロード用フォルダの内容を確認します。
2. 共有ルートの `app/` を更新します。コピー途中の `app/` を起動対象にしないため、可能なら `app.new/` として完全にコピーしてから、既存 `app/` と入れ替えます。
3. ルートの `YakuLingo起動.cmd`、`bootstrap.ps1`、`共有フォルダ配置手順.md` を更新します。
4. `manifest.json` は最後に配置します。bootstrap は manifest と実体が一致しない配布物をローカルへ導入しません。
5. 利用者のショートカットは共有ルートの `YakuLingo起動.cmd` を指す状態にします。作業フォルダはローカルパスにします。

## C. build ID とロールバック

- `app/config/build.txt` は manifest とローカルキャッシュの識別子です。共有側のフォルダ選択には使いません。
- 通常の内容更新では、同じ build ID でも manifest ハッシュにより別のローカルキャッシュになります。
- ロールバック用の版名フォルダは共有ルートへ併置しません。以前に保管した検査済みのフラット配布物を使い、Bの手順で `app/` とルートファイルを戻します。
- ロールバック用配布物も、現在の禁止資産検査と `Test-YakuPackage.ps1` を通したものだけを使用します。
- `Assert-YakuBuildIdentity` のフェイルファストは緩和しません。

## D. docsの扱い

- `app/README.md` と `app/DESIGN.md` は利用者・保守者向けのため配布物に残します。
- 開発ノートはリポジトリの `_docs/` に集約し、共有フォルダへ配置しません。
