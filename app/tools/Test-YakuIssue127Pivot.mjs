import fs from'node:fs';import path from'node:path';import{fileURLToPath}from'node:url';const a=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..'),r=(...p)=>fs.readFileSync(path.join(a,...p),'utf8'),server=r('src','Server.ps1'),q=r('www','quick.html'),j=r('www','assets','quick-page.js'),c=r('www','cat.html'),cj=r('www','assets','cat.js'),p=r('www','assets','premium-ui.js'),d=r('DESIGN.md'),f=[];const e=(v,m)=>{if(!v)f.push(m)};for(const id of['quick-purpose','quick-font','quick-font-size','quick-column-width','quick-lines','quick-wrap','quick-target-chars','quick-percent','quick-rewrite-actions','quick-variants'])e(q.includes('id="'+id+'"'),'missing '+id);e(j.includes("'/api/palette/chip'")&&j.includes('display_context:displayContext()'),'display-aware rewrite not wired');e(j.includes('yakuQuickHandoff')&&j.includes('yakuQuickReturn'),'handoff incomplete');e(c.includes('premium-apply-summary')&&c.includes('cat-open-align-entry'),'Excel entry/summary missing');e(p.includes('文章翻訳で補う')&&p.includes('consumeQuickReturn'),'round trip missing');e(!cj.includes("el('cat-resume').hidden")&&!cj.includes("el('cat-resume-list').innerHTML"),'legacy recent DOM used');e(!cj.includes("el('cat-candidates').hidden ="),'candidate null write remains');e(d.includes('Bilingual asset generation and reuse are separate trust boundaries'),'trust boundary missing');e(server.includes("$allowedChips = @('revise','shorten','shorter','rephrase','heading','table')"),'safe rewrite vocabulary missing');e(server.includes('厳密な収まり保証ではなく')&&server.includes('数字、単位、日付、社名、製品名'),'prompt constraints missing');if(f.length){f.forEach(x=>console.error('not ok - '+x));process.exit(1)}console.log('ok - Issue #127 UI and trust-boundary contract');